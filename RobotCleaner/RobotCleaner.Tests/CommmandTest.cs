using NUnit.Framework;

namespace RobotCleaner.Tests {
    [TestFixture]
    public class CommandTests {
        [Test]
        public void Execute() {
            //var command = new Command("E", 1);
            //IEnumerable<Point> points = command.Execute(new Point(1, 2));
            //Assert.That(points, Is.EquivalentTo())};
        }
    }
}